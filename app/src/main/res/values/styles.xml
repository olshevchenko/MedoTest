<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/logininfo_text_size</item>
    </style>

    <!-- Buttons -->
    <style name="ButtonStyle" parent="@android:style/Widget.Button">
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:layout_width">@dimen/button_width</item>
        <item name="android:layout_height">@dimen/button_height</item>
        <item name="android:textColor">@color/colorButtonText</item>
        <item name="android:textSize">@dimen/button_text_size</item>
        <item name="android:textStyle">normal</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
    </style>

    <style name="AccentButtonStyle" parent="@style/ButtonStyle">
        <item name="android:background">@color/colorAccent</item>
    </style>

    <style name="NormalButtonStyle" parent="@style/ButtonStyle">
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="ToggleButtonStyle" parent="@android:style/Widget.CompoundButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textOn"></item>
        <item name="android:textOff"></item>
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:background">@drawable/eyebutton</item>
    </style>


    <!-- EditTexts -->
    <style name="EditTextStyle" parent="@android:style/Widget.EditText">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textColorHint">@color/colorPrimaryTransparent</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:maxLines">1</item>
        <item name="android:singleLine">true</item>
    </style>

    <style name="PhoneNumberETStyle" parent="@style/EditTextStyle">
        <item name="android:inputType">phone</item>
        <item name="android:digits">"0123456789+ "</item>
        <item name="android:hint">@string/prompt_phone_number</item>
    </style>

    <style name="PasswordETStyle" parent="@style/EditTextStyle">
        <item name="android:inputType">textPassword</item>
    </style>

    <!-- Texts -->
    <style name="OnBoardingTextStyle" parent="@android:style/TextAppearance.Theme">
        <item name="android:textColor">@color/colorOnBoardingInfo</item>
        <item name="android:textSize">@dimen/onboardinginfo_text_size</item>
    </style>

    <style name="LoginTextStyle" parent="@android:style/TextAppearance.Theme">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/logininfo_text_size</item>
    </style>

    <style name="UserAgreementTextStyle" parent="@android:style/TextAppearance.Theme">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/useragreement_text_size</item>
    </style>

</resources>
